# raghavahuja.com

Personal site — senior design engineer. Static HTML/CSS/JS, no build step.

## Pages

- `index.html` — home (hero + live activity + now + selected work + lab + notes)
- `work.html` — work index
- `case-study.html?slug=arqo|futbolis|subway` — case study template
- `lab.html` — experiments
- `notes.html` — writing
- `about.html` — short bio + toolkit + beliefs
- `contact.html` — email + socials
- `404.html` — terminal (ls, cd, whoami, help)

## Shortcuts

- `⌘K` — command palette (routes, commands, fuzzy search, `chai`)
- `t` — toggle dark/light
- `g` (hold) — grid overlay
- `G` — pin grid overlay
- `↑↑↓↓←→←→ba` — konami, accent → CRT green + नमस्ते

## Stack

Zero dependencies. Three files: `index.html`, `site.css` (+ `tokens.css`), `site.js`.
Fonts: Geist + JetBrains Mono via Google Fonts.
Accent: Vercel blue `#0070F3`.

## Local

```
python3 -m http.server 8000
```

Open http://localhost:8000.
